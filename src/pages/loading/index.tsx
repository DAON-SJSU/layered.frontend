import * as _ from "./style";
import LoadingLogo from "../../components/loadingBox/index";

const Loading = () => {
  return (
    <_.Container>
      <LoadingLogo />
    </_.Container>
  );
};

export default Loading;
